import React from 'react';
import { ResponsiveLayout } from '../../components/ResponsiveLayout';

export const JournalLayout = ({ children }) => (
  <ResponsiveLayout nameModule="404">{children}</ResponsiveLayout>
);
