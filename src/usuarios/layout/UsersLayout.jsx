import React from 'react';
import { ResponsiveLayout } from '../../components/ResponsiveLayout';

export const UsersLayout = ({ children }) => (
  <ResponsiveLayout nameModule="Usuarios">{children}</ResponsiveLayout>
);
